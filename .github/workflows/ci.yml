name: examples-public-readiness

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inventory examples
        run: find examples -maxdepth 2 -type f | sort
